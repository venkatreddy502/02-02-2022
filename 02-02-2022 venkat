#include<stdio.h>
long int fact(int n)
{
	if((n==0)|| (n==1))
		{
			return(1);
		}
		else
		{
			return(fact(n-1)*n);
		}
}

int main()
{
	int n;
	printf("Enter number\t");
	scanf("%d",&n);
	if(n>0)
		printf("Factorial=%ld",fact(n));
	else
		printf(" n is not negative");
}
